For testing only. Do not use these rules for final production.

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

For real restaurant use, we should upgrade to Firebase Auth and stricter rules.
